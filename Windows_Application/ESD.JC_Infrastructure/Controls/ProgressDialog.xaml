<Window x:Class="ESD.JC_Infrastructure.Controls.ProgressDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        mc:Ignorable="d" 
        xmlns:prism="http://prismlibrary.com/"
        prism:ViewModelLocator.AutoWireViewModel="True"
        Height="92" Width="252" 
        WindowStyle="None" 
        WindowStartupLocation="CenterScreen"
        Closing="Window_Closing">
    <Grid>
        <Rectangle Fill="LightGray" Opacity="0.5"/>
        <Border BorderBrush="Black" BorderThickness="2" HorizontalAlignment="Center" VerticalAlignment="Center">
            <StackPanel Background="White" >
                <TextBlock Margin="20,20,20,10" Name="Message" Text="{Binding PDdescText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                <ProgressBar x:Name="ProgressBar" Margin="20,0,20,20" IsIndeterminate="True" Height="15" Width="200"></ProgressBar>
            </StackPanel>
        </Border>
    </Grid>
</Window>
